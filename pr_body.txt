**What I did:**
- Removed `@planned` tags from `features/text_input_select_validation.feature`.
- Fixed the URLs in the Playwright E2E tests `e2e/Select.spec.ts` and `e2e/TextInput.spec.ts` to point to the correct documentation paths (`/cobogo/docs/components/`), thereby resolving timeout failures.
- Created the new session log `2026-05-05T10-00-00Z_textinput-select-validation.md` logging the session context and BRASILIANA observations.

**Why:**
- The validation states for `TextInput` and `Select` correctly implement the Curva & Concreto aesthetic (`--vermelho-soft` and `--vermelho`) and the context integration with `FormField` works perfectly.
- To reflect the system's maturity, the `@planned` tags in the feature files needed to be removed (completing step 1 of the Underweight PR escalation ladder: "Implement existing planned steps").
- Correcting the URL paths ensures the E2E suite passes fully and provides robust visual governance.

**Decisions made:**
- Chose to fix the URLs in the test suite rather than deleting the tests, as visual governance must be maintained.
- Confirmed there are no regressions across build, Vitest logic, and Playwright DOM checks.

**Relevant prior logs:**
- [2026-05-04T23-45-00Z_notification-wcag-221.md](.jules/agent/brad_frost/2026-05-04T23-45-00Z_notification-wcag-221.md)

**Trade-offs:**
- None.

**Test evidence:**
- 100% pass on Vitest (128 passing).
- 100% pass on Playwright (22 passing) after fixing path timeouts.
- Astro static build succeeds with no related errors.
