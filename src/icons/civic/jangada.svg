<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="currentColor">
  <!-- Jangada -->
  <!-- Logs base -->
  <rect x="20" y="80" width="60" height="10" rx="5" />
  <rect x="25" y="70" width="50" height="10" rx="5" />

  <!-- Mast -->
  <rect x="45" y="10" width="5" height="60" />

  <!-- Sail -->
  <polygon points="50,15 90,65 50,65" />
</svg>
